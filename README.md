Configuration
All settings are changed via config.json in the project root — simulation duration, start time, and data file paths.

Results
A summary is printed to the console at the end of the simulation (shipment statuses, packed on time %, average pack time). A detailed event log is written to simulation.log — each line is a separate JSON object.